/**
 * Common resources used by the react components
 */

import * as React from "react";
import * as ReactDOM from "react-dom";
import { DrugMap } from "./components/DrugMap";

export default { React, ReactDOM, DrugMap };
